Possible vectors for hackers/bad actors. These are usually uncovered by vulnerability scans, and show you where your code is weak.

> It's important to note that commonly used code can cause multiple vulnerabilities where it's included. This means that fixing one part of your code can drastically reduce the amount of entry vectors significantly in some cases.