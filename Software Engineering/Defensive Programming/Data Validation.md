This is the simple test of asserting that your code actually changes data the way it's supposed to.

> `addTwoNumbers(x, y)` should return 2 given `(1, 1)`

This also includes sanitizing inputs and adding [[Guards|guards]] to functions.