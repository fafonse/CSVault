When a client and server exchange control messages before initializing data transfer.

> [[Hyper Text Transfer Protocol|HTTP]] makes a handshakes before data flow, where both sides use [[Cryptography#Types of Encryption|asymmetric key encryption]] before using a much faster symmetric key when they verify their identities.