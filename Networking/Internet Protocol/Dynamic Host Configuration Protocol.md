---
aliases:
  - DHCP
---
Obtains an IP address for a host attaching to an IP network.

The **DHCP** is used to automatically assign:

- IP addresses
- subnet masks
- default gateways
- DNS servers

to devices when they join a network.