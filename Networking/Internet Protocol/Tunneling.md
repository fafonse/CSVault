When you [[Encapsulation|encapsulate]] your data, and letting a different protocol handle the "shipping" of your data.

### IPV6
IPV6 can be transported through IPV4 protocols, as you can just change the header/encapsulate it and it will still work.
- IPV4 tunnels can carry IPV6 data.