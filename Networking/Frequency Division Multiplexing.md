Also known as *FDM*, this technique allows multiple channels/data streams to be transmitted on different frequency bands, keeping the data all separate.

This is the same technique that FM radio uses to separate different radio stations.