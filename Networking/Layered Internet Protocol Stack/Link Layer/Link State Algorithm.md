A "map" based approach to [[Routing Algorithm Classification]].

1. Discover neighbors and their costs
2. Share that info with all neighbors
3. Used shared info to run [[OSPF|shortest-path calculation]]

> Router knows the entire network, but at the cost of processing overhead.
> The dynamic version of this is [[Distance Vector Algorithm]]