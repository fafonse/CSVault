**Discrete values** are *stepped values*. Think like integers, where there is a noticeable jump between values. 

**Continuous values** are almost *analogue*. Think of floats/doubles, where (at least ideally) values can transition smoothly into each other with high precision. 