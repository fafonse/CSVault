The permissions your lambda functions can use. It's the *role* that your function plays.

When you create a new service in AWS, it automatically is created a role to give it some default permissions.

> Permissions are also called *policies*