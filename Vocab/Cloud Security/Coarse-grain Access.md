When access [[Permissions|permissions]] are unspecific and make users to access a far broader range of services than needed.

Generally unsecure, it is instead recommended to use [[Row Level Access|fine-grained access]].