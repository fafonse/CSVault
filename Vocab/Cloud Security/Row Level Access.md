When users have [[Permissions|permissions]] that are specific to the (ideal) point of only having access to specific *rows* in a table.

This reduces the [[Attack Surface|attack surface]] you present, as users are more limited to only access the software in the developer intended way.