How to get the min and max of an array.

```python
numbers = [1, 2, 3 ,4, 5]
np.min(numbers) # 1
np.max(numbers) # 5
np.mean(numbers) # 3
np.median(numbers) # 3

# These return the index of the min/max/mean
np.argmin(numbers) # 0
np.argmax(numbers) # 4
	
```