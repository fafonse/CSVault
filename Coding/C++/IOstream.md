## Output stream
Used mainly for the terminal, but can have multiple endpoints. Used for raw data too.

### Write
Writes raw data to the output stream, not text.  Needs a `char*` to work, and also requires the size of the amounts its streaming (in bytes).

## Input Stream