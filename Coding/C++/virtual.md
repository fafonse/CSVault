Virtual is a keyword used in classes for their methods.

Virtual only exists in the [[Header file|header]], and it makes subclasses call their overridden methods instead of their parent class's. It's required to fully utilize [[Polymorphism|polymorphism]].