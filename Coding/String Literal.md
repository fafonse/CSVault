A string where any special operators are made inneffective.

>[!example]
>`"C:\Users"` will parse out to "C:sers" or "C:Users", as the backslash is a special character.
>If using a string literal, it would come out as "C:\Users"