#todo
An algorithm that determines if a number is prime or not.

When it says yes, its 75% certain. In order to make sure that the number is prime, we can just run the test a lot to reduce the chance of error.